DELETE FROM LOCATIONS L WHERE L.LOCATION_ID IN (
    SELECT L2.LOCATION_ID
        FROM LOCATIONS L2
        JOIN COUNTRIES C2 on L.COUNTRY_ID = C2.COUNTRY_ID
    WHERE C2.COUNTRY_NAME = 'Switzerland'
    );

DELETE FROM COUNTRIES WHERE COUNTRY_NAME = 'Switzerland'