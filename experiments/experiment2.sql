CREATE TABLE stop1
  (
    log_id NUMBER GENERATED ALWAYS AS IDENTITY(START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    message VARCHAR2(50) not null
  );

CREATE TABLE stop2
  (
    log_id NUMBER GENERATED ALWAYS AS IDENTITY(START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    message VARCHAR2(50) not null
  );

CREATE TABLE stop3
  (
    log_id NUMBER GENERATED ALWAYS AS IDENTITY(START WITH 1 INCREMENT BY 1) PRIMARY KEY,
    message VARCHAR2(50) not null
  );

CREATE OR REPLACE TRIGGER STOP1
BEFORE INSERT
    ON stop1
BEGIN
    DBMS_OUTPUT.PUT_LINE('CALLING STOP 1');
    INSERT INTO stop2(message) values ('sth');
END;

CREATE OR REPLACE TRIGGER STOP2
BEFORE INSERT
    ON stop2
BEGIN
    DBMS_OUTPUT.PUT_LINE('CALLING STOP 2');
    INSERT INTO stop3(message) values ('sth');
END;

CREATE OR REPLACE TRIGGER STOP3
BEFORE INSERT
    ON stop3
BEGIN
    DBMS_OUTPUT.PUT_LINE('CALLING STOP 3');
    INSERT INTO stop1(message) values ('sth');
END;

INSERT INTO stop1(message) values ('msg');